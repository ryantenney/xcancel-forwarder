# Network Configuration
# Set these to match your local network setup

# Your timezone (for container logs)
TZ=America/New_York

# Macvlan Configuration (Optional - only if using macvlan networking)
# If you're using macvlan, uncomment and configure these values
# Otherwise, you can use standard bridge networking with port forwarding

# NETWORK_INTERFACE=eth0           # Your host's network interface (eth0, ens33, enp3s0, etc.)
                                    # Find with: ip link show
# LAN_SUBNET=192.168.1.0/24        # Your LAN subnet
# LAN_GATEWAY=192.168.1.1          # Your router/gateway IP
# NGINX_IP=192.168.1.100           # Dedicated IP for nginx container (must be free)
# IP_RANGE=192.168.1.64/28         # Small pool for containers (optional)
# HOST_IP=192.168.1.10             # Your Docker host IP (to exclude from pool)

# Standard Bridge Networking (Alternative to macvlan)
# Uncomment to use standard Docker networking with port forwarding
# HTTP_PORT=80
# HTTPS_PORT=443

# dnsmasq Configuration (Optional DNS server)
# Uncomment if you want to run the included dnsmasq DNS server
# DNSMASQ_IP=192.168.1.101         # IP for dnsmasq container (only with macvlan)
# UPSTREAM_DNS=1.1.1.1             # Your upstream DNS server
